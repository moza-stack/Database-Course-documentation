--Display all flight leg records--
SELECT * FROM flight leg;
--Display each airplane’s ID, type, and seat capacity--
SELECT 
    Airplane_id,
    Type_name AS Airplane_Type,
    Total_number_of_seats AS Seat_Capacity
FROM Airplane;
--List airplane IDs with seat capacity above 300--
SELECT Airplane_id
FROM Airplane
WHERE Total_number_of_seats > 300;
--Display airport codes and names where city = 'Cairo--
SELECT airport_ID AS AirportCode, name AS AirportName
FROM airport
WHERE city = 'Cairo';
-- Display airplanes whose type contains 'Boeing'.--
SELECT *
FROM Airplane
WHERE Type_name LIKE '%Boeing%';
-- Insert a customer with NULL contact number--
INSERT INTO Customer (Customer_id, name, phone)
VALUES (1, 'John Doe', NULL);
--Increase available seats by 10 for all domestic flights.--
UPDATE FlightLeg
SET AvailableSeats = AvailableSeats + 10
WHERE FlightType = 'Domestic';
--Update airplane seat capacity by +20 where capacity < 150--
UPDATE Airplane
SET Total_number_of_seats = Total_number_of_seats + 20
WHERE Total_number_of_seats < 150;

